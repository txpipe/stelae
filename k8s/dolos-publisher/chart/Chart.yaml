apiVersion: v2
name: dolos-publisher
description: one Stelae publisher Job for one network repository
type: application
# Chart version: this chart. There is no appVersion on purpose: the publisher
# build a release runs is image.tag in the network's values file, and the
# three networks need not agree.
version: 0.2.0
home: https://github.com/txpipe/stelae
keywords:
  - stelae
  - oci
  - publisher
